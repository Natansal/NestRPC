/**
 * Used to store the router options
 */
export const ROUTER_METADATA = Symbol("ROUTER_METADATA");
/**
 * Used to store the route options
 */
export const ROUTE_METADATA = Symbol("ROUTE_METADATA");
